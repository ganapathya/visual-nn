# Lightweight requirements for EC2 free tier
# CPU-only PyTorch (much smaller than CUDA version)
torch>=2.0.0
torchvision>=0.15.0
--index-url https://download.pytorch.org/whl/cpu

# Core dependencies
Flask>=2.3.0
Flask-CORS>=6.0.0
Pillow>=10.0.0
numpy>=1.24.0

# Production server
gunicorn>=21.0.0

# Optional: For even lighter installation, uncomment one of these alternatives:
# scikit-image>=0.20.0  # Alternative to PyTorch for image processing
# opencv-python>=4.8.0  # Alternative to PyTorch for image processing
